# 학기 시간표 갱신 절차 (swap-finder2)

새 학기가 시작되면 사이트의 시간표를 교체해야 한다. 이 문서는 그 방법과,
2026학년도 2학기 갱신 때 실제로 걸렸던 함정들을 기록한 것이다.

> **먼저 읽을 것**: 아래 「함정」 절은 추측이 아니라 실제로 틀렸던 사례들이다.
> 여기를 건너뛰면 같은 자리에서 같은 시간을 쓰게 된다.

---

## 1. 시스템 구조

사이트는 데이터 두 개를 합쳐서 동작한다.

| | 무엇 | 어디 | 갱신 주기 |
|---|---|---|---|
| **학기 시간표** | 누가 · 어느 반 · 무슨 과목 | `index.html`의 `BUILTIN_TEACHERS` | 학기마다 (이 문서의 대상) |
| **실시간 데이터** | 특정 날짜에 실제 수업이 있는지 | 컴시간 API → `worker.js` | 자동 |

**오늘부터 2주까지**는 실시간 데이터를 쓰고(수업교환 반영됨), **2주를 넘어가면**
학기 시간표로 넘어간다. 그래서 학기 시간표가 틀리면 2주 뒤 날짜부터 조용히 틀린 답이 나온다.

### 교사 이름이 붙는 방식 — 가장 깨지기 쉬운 부분

컴시간은 교사 이름을 주지 않고 **순번만** 준다. `worker.js`의 `TEACHER_NAMES`
배열이 그 순번에 이름을 붙이는 이름표다.

**배열 길이가 컴시간의 `교사수`와 정확히 같아야 한다.** 하나라도 어긋나면
그 지점부터 뒤쪽 교사 전원의 시간표가 다른 사람 이름으로 표시된다.
2학기 갱신 전에 실제로 이 상태였다(38 vs 36).

---

## 2. 컴시간 API

### 받는 법

```
http://comci.net:4082/36179_T?NzM2MjlfODcxNjNfMF8x
```

쿼리스트링은 `base64("73629_87163_0_" + r)` 이고 `r=1`이 이번 주, `r=2`가 다음 주다.
`worker.js` 상단의 `SC`, `SCHOOL_CODE` 상수와 대응한다.

- 평문 HTTP 4082 포트라 대부분의 자동 조회 도구가 접근하지 못한다.
  **브라우저에서 열고 `Ctrl+S` → 「웹페이지, HTML만」으로 저장**하는 것이 가장 확실하다.
- 화면에 글자가 깨져 보여도 상관없다. 저장된 바이트는 UTF-8로 멀쩡하다.
- 응답 뒤에 널바이트가 잔뜩 붙어 있다. `json.JSONDecoder().raw_decode()`로 앞부분만 파싱한다.

### 키 구조

| 키 | 내용 | 비고 |
|---|---|---|
| `교사수` | 38 | **자율·창체 가상 항목 포함** |
| `자료446` | 교사명 배열 | 1-based, `김미*`처럼 마스킹됨, 끝 2개는 자율/창체 |
| `자료492` | 과목 약어 배열 | 1-based, `[0]`은 개수 |
| `자료481` | **기본 시간표** (학급별) | ← 기준으로 삼을 것 |
| `자료147` | 이번 주 실제 시간표 | `>` 접두사 = 변경된 칸. **기준으로 쓰면 안 됨** |
| `자료542` | 교사별 날짜별 시간표 | `worker.js`가 쓰는 것. 행 번호 = 교사 번호 |
| `담임` | 학년별 담임 교사번호 | `담임[학년-1][반-1]` |
| `시작일` | 해당 주 월요일 | |
| `일과시간` | 교시별 시작 시각 | |

### 값 인코딩

```
학급별 (자료481, 자료147):  과목번호 × 1000 + 교사번호
교사별 (자료542):           과목번호 × 1000 + 학급코드(학년×100 + 반)
```

문자열로 오는 값(`">40035"`)은 `>`를 떼고 정수로 바꾼다.

중첩 구조는 길이가 앞에 붙는 형태다.

```
자료481 = [학년수, 학년1, 학년2, ...]
  학년n = [반수, 반1, 반2, ...]
    반  = [요일수, 월, 화, 수, 목, 금]
      요일 = [교시수, 1교시값, 2교시값, ...]
```

---

## 3. 갱신 절차

### 준비물

1. **주간시간표 xlsx** — 교사별 주간 시간표 (반번호 + 과목약어)
2. **시수 배정표 xlsx** — 담당번호 · 교사 실명 · 과목 정식명 · 학급별 시수
3. **컴시간 원본** — 위 URL로 받은 JSON (교사 명단이 바뀌었을 때 필수)

### 순서

1. **교사 명단 확정**
   `자료446`의 마스킹 이름과 배정표의 실명을 **앞 두 글자 + 순서**로 대조한다.
   `자료446` 길이 = `교사수`이고, 여기에 자율·창체가 포함되어 있음을 확인한다.

2. **과목 약어 → 정식명**
   `자료492`(또는 주간시간표)의 약어를 교사별로 배정표 과목 목록과 대조한다.
   교사 한 명의 약어 개수와 배정표 과목 개수가 같으면 담당 학급·시수로 1:1 결정된다.
   못 정한 것만 사람에게 물어본다.

3. **시간표 생성**
   슬롯 형식은 반드시 아래와 같아야 한다. `index.html`의 `parseCls` 정규식 세 개를
   모두 통과해야 하기 때문이다.

   ```
   {학년}학년 {학과}/{과목명}({반})
   예) 1학년 경영정보과/통합과학2(1)
   ```

   자율·창체는 학급이 없으므로 `자율활동` / `창의적 체험활동`만 넣는다.
   `NOSWAP` 집합이 이 두 문자열로 교체를 차단한다.

   반번호 → 학과 매핑 (**학과 개편 시 반드시 재확인**):

   ```
   1, 2 → 경영정보과    3 → 뷰티미용과
   4    → 조리과        5, 6, 7 → 관광비즈니스과
   ```

4. **검증** — `tools/verify_timetable.py` 실행 (4절 참고)

5. **파일 교체**
   - `index.html` — `BUILTIN_TEACHERS` 한 줄 통째로 교체
   - `worker.js` — `TEACHER_NAMES` 배열 교체 (자율·창체 포함)

6. **배포**
   - 커밋 → **사용자가 윈도우에서 `git push`**
   - Cloudflare 대시보드에서 `worker.js` 붙여넣고 Deploy

7. **실제 날짜로 확인**
   - 2주 이내 날짜 → 실시간 데이터가 반영되는가
   - 2주 초과 날짜 → 새 학기 과목명이 나오는가
   - 자율·창체 교시 → 교체 후보 0명인가

---

## 4. 검증 기준

`tools/verify_timetable.py`가 아래를 자동으로 확인한다.

- 교사 수가 배정표와 일치
- **교사별 주당 시수가 배정표 선언값과 전원 일치** ← 가장 강력한 검증
- 생성된 시간표가 컴시간 `자료481`과 칸 단위로 일치
- 모든 슬롯이 `parseCls` 정규식을 통과
- 매핑되지 않은 약어가 없음

시수 검증이 36/36으로 통과하면 사실상 안심해도 된다. 2학기 갱신 때
주간시간표 xlsx와 `자료481`이 11% 달랐지만, **시수 합계는 양쪽 모두 배정표와 일치**했다.
배치가 다른 것은 판본 차이이지 오류가 아니다.

---

## 5. 함정

2026학년도 2학기 갱신에서 실제로 걸렸던 것들이다.

### 데이터

- **배정표의 담당번호를 믿지 말 것.** 2학기 배정표에는 32번이 두 명이고 34번이 없었다.
  번호는 참고만 하고 이름과 순서로 대조한다.
- **`자료446` 끝의 2개는 교사가 아니다.** 「자율」·「창체」 가상 항목이다.
  `TEACHER_NAMES`에서 빼면 인덱스가 밀린다. 반드시 포함할 것.
- **앞 두 글자가 같은 교사에 주의.** 신동관/신동숙, 박미수/박미애 같은 경우
  마스킹된 이름(`신동*`)만으로는 구분되지 않는다. 순서로 갈라야 한다.
- **`자료147`을 기준으로 쓰지 말 것.** 이번 주 실제 시간표라서 결강·보강이 섞여 있다.
  교사 한 명이 0시수로 나오기도 한다(출장 등). 기본 시간표는 `자료481`이다.
- **팀티칭 때문에 조리·미용 교사끼리 시간표가 겹친다.** 실시간 데이터만으로
  누가 누구인지 추론하려 하면 틀린다. 명단은 `자료446`으로 확정할 것.

### 도구

- **큰 JSON 배열을 웹 조회 도구로 읽지 말 것.** 38행짜리 배열을 옮겨 적다가
  줄이 빠져서, 인덱스가 한 칸씩 밀린다는 잘못된 결론에 도달했다.
  **반드시 파일로 받아 직접 파싱한다.**
- **xlsx 셀에 `_x000D_` 문자열이 섞여 있다.** openpyxl로 읽을 때 제거해야 한다.
- **`index.html`은 줄바꿈이 CRLF다.** 편집할 때 유지하지 않으면 diff가 1000줄로 부풀어 오른다.

### git

- **마운트된 폴더에서 git이 `index.lock`을 지우지 못한다.** 매 명령이 실패한다.
  삭제 권한을 먼저 받아야 한다(`device_request_delete_permission`).
- **커밋 전에 원격을 확인할 것.** 2학기 갱신 때 원격에 모르는 커밋(안내서·이미지)이
  있어서, 그대로 올렸다면 충돌하거나 그 작업이 날아갈 뻔했다.
- **push는 사용자가 윈도우에서 해야 한다.** 리눅스 작업공간은 프록시가 GitHub를
  차단하고(403), 클라우드 쪽은 이 저장소에 자격증명을 붙여주지 않는다.

---

## 6. 참고

- 사이트: https://egames22.github.io/swap-finder2/
- API: https://swap-proxy.eastward2.workers.dev/api/schedule
- Worker: Cloudflare 대시보드 → Workers & Pages → `swap-proxy`

### 자동화를 하지 않기로 한 이유

배정표 양식과 시간표 형식이 학기마다 달라질 수 있어서, 파서를 고정하면
**조용히 틀린 시간표가 배포되는** 쪽이 더 위험하다고 판단했다.
대신 이 문서와 검증 스크립트를 남겨, 매번 사람이 확인하며 진행하기로 했다.

*최초 작성: 2026학년도 2학기 갱신 작업 중*
